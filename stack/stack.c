step1 esp points to the return address
    esp is technically at the bottom of the stack always
    esp IS the bottom of the stack
    the rule is: whenever you are at the top of a new function, esp is at the return address
step2 
